@Library('testing_repo') _  // Import the shared library

pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'jenkins', url: 'https://github.com/vedantsharmascaler/testing_repo.git'
            }
        }

        stage('Use Shared Library') {
            steps {
                printMessage('Hello, Vedant! Using Jenkins Shared Library.') // Call function
            }
        }
    }
}
